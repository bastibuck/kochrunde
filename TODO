
Todo:
  ☐ use imageBuilder 
      sanity provides a image builder https://www.sanity.io/docs/presenting-images 
      that takes a image asset and gets the URL for it and allows cropping, sizes etc. 
        it needs the client though... so how do we keep secrets??

  ☐ mehrere rezeptlinks erlauben
  ☐ mehrere bilder
  ☐ codegen for sanity docs